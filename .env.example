# Copier en .env.local pour le développement (jamais commité, voir .gitignore).
# Sur Vercel : Project Settings -> Environment Variables.

# Clé d'API Govee -- Govee Home app -> Profil -> Réglages -> "Apply for
# API Key" (délivrée par email, généralement sous 24 h). Reste côté
# serveur uniquement (src/lib/govee/client.ts) : ne jamais l'exposer via
# NEXT_PUBLIC_.
GOVEE_API_KEY=
CITY=
LATITUDE=
LONGITUDE=